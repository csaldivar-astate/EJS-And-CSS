<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listings</title>
    <link rel="stylesheet" href="css/listings.css">
</head>
<body>
    <ul class="listingsContainer">
        <% for ( const listing of listings ) { %>
            <a href="/listings/<%= listing.listingID %>" class="listing">
                <li>
                    <div class="row">
                        <p><%= listing.firstName + " " + listing.lastName %></p>
                        <p><%= listing.email %></p>
                    </div>
                    <div class="row">
                        <p><%= listing.make + " " + listing.model + " " + listing.year %></p>
                        <p>$<%= listing.price %></p>
                    </div>
                </li>
            </a>
        <% } %>
    </ul>
</body>
</html>
